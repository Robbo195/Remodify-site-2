import React from 'react';
import { Container } from 'react-bootstrap';

const Returns = () => (
  <Container style={{ padding: '40px 0' }}>
    <h2>Returns</h2>
    <p>This is a placeholder for the Returns policy page.</p>
  </Container>
);

export default Returns;
