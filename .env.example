# Email configuration
# Add this file to .gitignore - do not commit credentials

# SMTP (nodemailer) settings - leave blank if using Microsoft Graph
EMAIL_SERVICE=Outlook
EMAIL_USER=
EMAIL_PASS=
EMAIL_RECIPIENT=

# Microsoft Graph settings (recommended for Microsoft 365)
# Set USE_GRAPH=true to enable Graph-based sending (requires an Azure app registration
# with the "Mail.Send" application permission and admin consent).
USE_GRAPH=true
# AZURE_TENANT_ID=
# AZURE_CLIENT_ID=
# AZURE_CLIENT_SECRET=
# The mailbox (userPrincipalName) the app will send as
GRAPH_SENDER=no-reply@modrex.com.au
